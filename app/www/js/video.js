const videoPath = 'https://thegrid.training/assets/videos/spot.mp4'

const initialize = () => { 

    let path = this.filePath.resolveNativePath( this.file.applicationDirectory + "www/" + assetsFilePath )

    console.log( path )

}

export default initialize