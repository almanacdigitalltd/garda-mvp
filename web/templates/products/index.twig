{% requireLogin %}

{% extends "_layouts/_base" %}

{% set products = craft.entries()
    .section('products')
    .productCategory(category)
    .all()
%}

{% block content %}

    {% include "_layouts/_header" with {
        title: "Training - Centre",
        subTitle: category.title,
        subTitleWeight: 'medium'
    } %}

    <div class="o-main o-main--wide o-main--shadow">

        <div class="c-product-list">

        {% for product in products %}
            {% set productImage = product.productImage.one() %}

            <div class="c-product-list__item">
            
                <div class="c-product-list__meta">

                    {% if productImage %}
                    <div class="c-product-list__image">
                        {{ productImage.getImg() }}

                    </div>
                    {% endif %}
                    
                    <div class="c-product-list__data">
                        <a class="c-product-list__info" href="#"></a>
                        <a class="c-product-list__download{% if loop.index == 2 %} c-product-list__downloaded{% endif %}" href="#"></a>
                    </div>

                </div>
                
                <div class="c-product-list__title">
                    <h2 class="fw-medium">
                        <a class="c-product-list__link" href="{{ product.url }}">
                            <span class="c-product-list__status{% if product.slug == 'night-fighter' and currentUser.testPass %} c-product-list__status--passed{% else %} c-product-list__status--failed{% endif %}{% if loop.index == 1 or loop.index == 3 %} c-product-list__status--passed{% else %} c-product-list__status--failed{% endif %}"></span>
                            {{ product.title }}
                        </a>
                    </h2>
                </div>
                
                <div class="c-product-list__brand">
                    <h3 class="h2 fw-medium">{{ product.productBrand }}</h3>
                </div>
                
            </div>
            
        {% endfor %}

        </div>

    </div>

    {% set footerSettings = {
        takeQuizHide: true
    } %}

    {% if currentUser.can('editUsers') %}
        {% set footerSettings = footerSettings|merge({
            manageUsersShow: true
        }) %}
    {% endif %}

    {% include "_layouts/_footer" with footerSettings %}

{% endblock %}